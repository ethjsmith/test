This directory is where I store things that I am working on or testing, like features for bigger
projects. probably it will be ( see -> IS ) a cluttered mess of garbage, but whatever, garbage is fun
Ill try to make subfolders that describe what the project is actually doing, which might help a bit
